"""App configuration for duck_api."""

from django.apps import AppConfig


class DuckApiConfig(AppConfig):
    """Configuration for duck_api Django application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "backend.duck_api"
